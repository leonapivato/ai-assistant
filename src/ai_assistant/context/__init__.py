"""Context: assembles the situational context for a request.

Gathers the "right now" — time, location, calendar, active tasks, current
situation — and combines it with retrieved memory into the working context the
orchestration engine hands to the model. Owns *what* the assistant is aware of
at the moment of a request.

Contract: TBD (added here as this subsystem is designed).
"""
